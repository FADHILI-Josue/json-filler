{
  "name": "json-filler",
  "version": "2.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:content": "esbuild src/content.ts --bundle --outfile=build/content.js --format=iife --platform=browser",
    "build:background": "esbuild src/background.ts --bundle --outfile=build/background.js --format=esm",
    "build:popup": "esbuild src/popup.ts --bundle --outfile=build/popup.js --format=iife --platform=browser",
    "build": "npm run build:content && npm run build:background && npm run build:popup"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@10.14.0",
  "devDependencies": {
    "@types/chrome": "^0.1.32",
    "esbuild": "^0.27.1",
    "typescript": "^5.9.3"
  }
}
